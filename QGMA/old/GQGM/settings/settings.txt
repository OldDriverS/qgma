# 本配置文件中涉及单个设置多项的配置，
# 每个配置中会说明项数上限，超出部分无效，
# 每项用空格隔开，示例：12345 67890
# 对应设置为空则默认不启用对应的功能




# 基本设置 #
[basic]
# 机器人QQ号
bot_user_id = 
# 报告管理员，用于接收群聊异常消息报告，需要与机器人QQ为好友关系，
# 不填写则视为不接收报告消息
# 最多设置3个，每项用空格隔开
report_admin = 
# 高级管理员，用于管理机器人，运行高级命令，
# 最多设置3个，每项用空格隔开
senior_admin = 
# 普通管理员，只能管理机器人，运行普通命令，
# 最多设置6个，每项用空格隔开
general_admin =
# 报告周期
report_cycle = 
# 结算周期
settle_cycle =


# 群聊设置 #
[group]
# 需要机器人管理的QQ群的群号，
# 最多设置8个，每项用空格隔开
managed_group = 
# 群聊定时禁言的开始时间和结束时间，
# 需要设置2项，每项用空格隔开
# 时间每项必须为4个数字
# 示例：0000 0600为早上0点到早上6点全体禁言
# 1800 1500为下午6点到第2天下午3点全体禁言
group_whole_ban = 


# 频道设置 #
[guild]
# 需要机器人管理的QQ频道的频道号，
# 最多设置8个，每项用空格隔开
managed_guild = 
# 频道定时禁言的开始时间和结束时间，
# 需要设置2项，每项用空格隔开
# 时间每项必须为4个数字
# 示例：0000 0600为早上0点到早上6点全体禁言
# 1800 1500为下午6点到第2天下午3点全体禁言
guild_whole_ban = 





# 设置向导
[wizard]
# 设置文件配置向导的状态，向导将用于协助您第一次配置设置，
# 默认由程序管理，如果您已经在首次启动前配置了设置文件，
# 请将值修改为0，如需重新启动此向导，请将值修改为1
current_step = 1